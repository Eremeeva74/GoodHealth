<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoodHealth"
        tools:targetApi="31">
        <!--
        <activity
            android:name=".MNOItem"
            android:exported="false"
            android:label="@string/title_activity_mnoitem"
            android:theme="@style/Theme.GoodHealth" />
        <activity
            android:name=".PressureActivity"
            android:exported="false"
            android:label="@string/title_activity_pressure"
            android:theme="@style/Theme.GoodHealth" />
        <activity
            android:name=".MainScreen"
            android:exported="false"
            android:label="@string/title_activity_main_screen"
            android:theme="@style/Theme.GoodHealth" />
        <activity
            android:name=".MNOScreen"
            android:exported="false"
            android:label="@string/title_activity_mnoscreen"
            android:theme="@style/Theme.GoodHealth" />
        <activity
            android:name=".MNOActivity"
            android:exported="false" />
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GoodHealth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>